fastapi>=0.115.0
# Multipart form parsing - what POST /send uses to accept an uploaded file.
# Starlette raises at request time without it, so it is a hard dependency.
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0
pymongo>=4.13.0
httpx>=0.28.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
